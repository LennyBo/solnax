export interface ArrowPoint {
  x: number;          // Percentage from left (0-100)
  y: number;          // Percentage from top (0-100)
  value: number;      // Determines color/direction in the child
  rotation: number;   // Manual setting
}
